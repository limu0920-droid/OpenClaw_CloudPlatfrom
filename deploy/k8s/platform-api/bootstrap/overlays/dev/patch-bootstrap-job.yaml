apiVersion: batch/v1
kind: Job
metadata:
  name: openclaw-platform-api-bootstrap
spec:
  template:
    spec:
      containers:
        - name: bootstrap
          image: openclaw-dev-platform-api:latest
          imagePullPolicy: IfNotPresent

